<Dialog> {/* dialog */}
  <Form> {/* form + react-hook-form + zod */}
    <Select /> {/* select category */}
    <Switch /> {/* switch is_active */}
  </Form>
</Dialog>
<AlertDialog> {/* alert-dialog สำหรับ delete */} </AlertDialog>
